const fetch = require('isomorphic-fetch');
const Promise = require('bluebird');

const server = 'https://api.parse.com/1/classes/chatterbox/';

const requestChats = {};
module.exports = requestChats;

requestChats.getRound = (skip) => {
	//
}

requestChats.getAll = () => {
	//
}

